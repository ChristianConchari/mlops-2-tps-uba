# Mini-TP 2 — Metadatos del modelo por GraphQL (Sesión 2)

Expone los metadatos del modelo del [Mini-TP 1](../tp1/) por **GraphQL**
(FastAPI + Strawberry) y compara la misma lectura contra el endpoint REST.

## Contenido

- `mini_tp2_actividad.ipynb` — notebook con el esquema GraphQL (`Model`, `Metrics`),
  la query, el cliente Python y la comparación REST vs GraphQL.

## Cómo correr

```bash
uv pip install strawberry-graphql fastapi "uvicorn[standard]" requests
# abrir el notebook y ejecutar de arriba a abajo
```

## Comparación REST vs GraphQL

_(pendiente: llamadas y bytes para armar la misma vista + conclusión)_
